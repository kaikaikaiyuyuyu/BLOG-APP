generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Blog {
  id        Int      @id @default(autoincrement())
  title     String
  date      DateTime
  content   String
}
